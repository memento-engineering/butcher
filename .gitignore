.dart_tool/
.idea/

# One resolution for the whole workspace, so the only lock file is this one.
pubspec.lock

# Run artifacts: a member is a working directory now, so these are not anchored
# to the workspace root.
mutation-report.json
self-run.log

# the_grid live-arm worktrees + station artifacts
.grid/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
