/// Tool version embedded in log events.
///
/// `test/version_test.dart` fails if this and the `version` field of this
/// package's `pubspec.yaml` disagree, so the two cannot drift apart again.
const packageVersion = '0.1.0';
