# Replaces .gitignore for `dart pub publish`, so it repeats the entries that
# can appear inside this member. The lock file and the maintainer-only trees
# live at the workspace root, outside the published archive already.
.dart_tool/
/mutation-report.json
/self-run.log
