name: butcher_report
description: The Stryker mutation-report schema, so a consumer can read or write the report without the mutation engine.
version: 0.1.0
repository: https://github.com/memento-engineering/butcher

environment:
  sdk: ^3.12.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
